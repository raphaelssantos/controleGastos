= simple_form_for(@despesa) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nomedespesa
    = f.input :datavencimento
    = f.input :valor
    = f.input :saldo

  .form-actions
    = f.button :submit
