%h1 Listing receitas

%table
  %thead
    %tr
      %th Nomereceita
      %th Datavencimento
      %th Valor
      %th Saldo
      %th
      %th
      %th

  %tbody
    - @receitas.each do |receita|
      %tr
        %td= receita.nomereceita
        %td= receita.datavencimento
        %td= receita.valor
        %td= receita.saldo
        %td= link_to 'Show', receita
        %td= link_to 'Edit', edit_receita_path(receita)
        %td= link_to 'Destroy', receita, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Receita', new_receita_path
